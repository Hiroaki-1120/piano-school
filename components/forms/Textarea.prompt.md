Multi-line input for review text and studio inquiry messages.

```jsx
<Textarea label="口コミを書く" rows={5} hint="先生やレッスンの雰囲気を教えてください" />
```

Same label/hint/error contract as `Input`. Vertically resizable.
