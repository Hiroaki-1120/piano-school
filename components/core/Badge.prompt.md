Status / category pill. Soft-tinted by default; `solid` for emphasis.

```jsx
<Badge tone="success">体験レッスンあり</Badge>
<Badge tone="primary" solid>NEW</Badge>
```

Tones: `neutral`, `primary`, `accent`, `success`, `info`, `danger`. For interactive filter chips use `Tag` instead.
