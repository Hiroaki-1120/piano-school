Review rating display with fractional star fill (honey/amber).

```jsx
<RatingStars value={4.6} count={28} />
<RatingStars value={5} size={14} showValue={false} />
```

Supports partial fill (4.6 fills 92% of stars). Pass `count` to show （N件）. Read-only display — not an input.
