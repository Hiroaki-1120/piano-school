Generic surface container. `interactive` adds a gentle hover lift for clickable cards.

```jsx
<Card>本文…</Card>
<Card interactive onClick={open}>クリックできるカード</Card>
```

For the studio listing tile use the purpose-built `StudioCard` instead.
